# =============================================================================
# FAHRDIENST ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!

# =============================================================================
# SUPABASE (Required)
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Service Role Key (for admin operations like seeding demo users)
# Find in: Supabase Dashboard → Settings → API → service_role (secret)
# WARNING: Never expose this key in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# GOOGLE MAPS (Required for route calculation)
# =============================================================================
# Client-side key (for Places Autocomplete and Maps display)
# Restrict to: Maps JavaScript API, Places API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-client-key

# Server-side key (for Directions and Distance Matrix API)
# Restrict to: Directions API, Distance Matrix API
# If not set, uses client key (not recommended for production)
GOOGLE_MAPS_SERVER_API_KEY=your-google-maps-server-key

# =============================================================================
# SMS NOTIFICATIONS (Optional - Twilio)
# =============================================================================
# Set SMS_ENABLED=true to enable SMS sending
SMS_ENABLED=false
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
# Phone number in E.164 format (e.g., +41791234567)
TWILIO_FROM_NUMBER=+41xxxxxxxxx

# =============================================================================
# EMAIL NOTIFICATIONS (Optional)
# =============================================================================
# Set EMAIL_ENABLED=true to enable email sending
EMAIL_ENABLED=false
# SMTP configuration (or use Supabase Edge Functions)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
EMAIL_FROM=noreply@fahrdienst.ch

# =============================================================================
# SECURITY (Production)
# =============================================================================
# Set to production URL for proper CORS handling
# NEXT_PUBLIC_SITE_URL=https://fahrdienst.example.com

# =============================================================================
# TESTING (E2E Tests)
# =============================================================================
# Test user credentials for Playwright E2E tests
# Create a test user in Supabase with appropriate role
TEST_USER_EMAIL=test-dispatcher@example.com
TEST_USER_PASSWORD=your-test-password

# =============================================================================
# DEVELOPMENT OPTIONS
# =============================================================================
# Enable verbose logging (development only)
# DEBUG=true
